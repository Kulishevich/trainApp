export type NoteElemType = {
    id: number,
    title: string,
    note: string,
    status: boolean
  }

export type ProgramElemType = {
    id: number,
    title: string,
    text: string,
    img: string
}

export type ResultElemType = {
    id: number,
    category: string,
    exercise: string,
    result: string,
    maxResult: string
}